export const AuthErrorMessages = {
  GOOGLE_LOGIN_NO_PASSWORD: 'You must manually register with an e-mail and password, if you have previously logged in with Google we will automatically link your accounts',
} as const;
